let express = require('express');
let app = express();

const PORT = process.env.PORT || 3030;

function(req, res) {
  res.send('Hello Express');
}































 module.exports = app;
